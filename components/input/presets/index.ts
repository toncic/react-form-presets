import EmailPreset from "./email.preset";
import IntegerPreset from "./integer.preset";
import PasswordPreset from "./password.preset";

const Presets = {
  Email: EmailPreset,
  Password: PasswordPreset,
  Integer: IntegerPreset
}

export default Presets;