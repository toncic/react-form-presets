import isEmailValid from "./email.validation";
import isPasswordValid from "./password.validation";
import { isInt } from "./number.validation";

const Validation = {
  isEmailValid,
  isPasswordValid,
  isInt,
}

export default Validation;